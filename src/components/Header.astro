---
import ThemeToggle from './ThemeToggle.astro';

const navLinks = [
  { href: '/', label: 'Home' },
  { href: '/projects', label: 'Projects' },
  { href: '/blog', label: 'Blog' },
  { href: '/about', label: 'About' },
];

const currentPath = Astro.url.pathname;
---

<header class="w-full border-b border-[--color-sage]/15">
  <nav class="max-w-5xl mx-auto px-6 py-4 flex items-center justify-between">
    <!-- Logo -->
    <a href="/" class="group flex items-center gap-2 no-underline">
      <svg viewBox="0 0 32 32" fill="none" class="w-8 h-8" aria-hidden="true">
        <circle cx="16" cy="16" r="15" fill="currentColor" class="text-[--color-midnight] [html[data-theme=light]_&]:text-[--color-surface-light]" stroke="var(--color-teal)" stroke-width="0.5" stroke-opacity="0.3"/>
        <path d="M 4,24 L 10,14 L 14,18 L 16,24" fill="var(--color-teal)" opacity="0.3"/>
        <path d="M 16,24 L 18,18 L 22,14 L 28,24" fill="var(--color-teal)" opacity="0.3"/>
        <path d="M 8,24 L 16,8 L 24,24 Z" fill="var(--color-teal)"/>
        <path d="M 13.5,12 L 16,8 L 18.5,12" stroke="var(--color-amber)" stroke-width="1.5" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
      <span class="font-display text-lg font-bold text-[--color-text-dark] [html[data-theme=light]_&]:text-[--color-text-light] group-hover:text-[--color-teal] transition-colors">
        kafkade
      </span>
    </a>

    <!-- Navigation -->
    <div class="flex items-center gap-6">
      <ul class="hidden sm:flex items-center gap-6 list-none m-0 p-0">
        {navLinks.map(({ href, label }) => (
          <li>
            <a
              href={href}
              class:list={[
                'text-sm transition-colors no-underline',
                currentPath === href || (href !== '/' && currentPath.startsWith(href))
                  ? 'text-[--color-teal] font-medium'
                  : 'text-[--color-sage] hover:text-[--color-teal-light]',
              ]}
            >
              {label}
            </a>
          </li>
        ))}
      </ul>
      <ThemeToggle />

      <!-- Mobile menu button -->
      <button
        id="mobile-menu-btn"
        class="sm:hidden text-[--color-sage] hover:text-[--color-teal] transition-colors"
        aria-label="Toggle menu"
      >
        <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </button>
    </div>
  </nav>

  <!-- Mobile navigation -->
  <div id="mobile-menu" class="hidden sm:hidden border-t border-[--color-sage]/15">
    <ul class="flex flex-col px-6 py-4 gap-3 list-none m-0 p-0">
      {navLinks.map(({ href, label }) => (
        <li>
          <a
            href={href}
            class:list={[
              'text-sm block py-1 no-underline',
              currentPath === href || (href !== '/' && currentPath.startsWith(href))
                ? 'text-[--color-teal] font-medium'
                : 'text-[--color-sage] hover:text-[--color-teal-light]',
            ]}
          >
            {label}
          </a>
        </li>
      ))}
    </ul>
  </div>
</header>

<script>
  const btn = document.getElementById('mobile-menu-btn');
  const menu = document.getElementById('mobile-menu');
  btn?.addEventListener('click', () => {
    menu?.classList.toggle('hidden');
  });
</script>
